---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: rulegroups.alerting.gf.m.crossplane.io
spec:
  group: alerting.gf.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gf
    kind: RuleGroup
    listKind: RuleGroupList
    plural: rulegroups
    singular: rulegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RuleGroup is the Schema for the RuleGroup API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleGroupSpec defines the desired state of a RuleGroup.
            properties:
              forProvider:
                description: RuleGroupParameters are the configurable fields of a
                  RuleGroup.
                properties:
                  disableProvenance:
                    default: true
                    description: DisableProvenance allows modifying the rule group
                      from the Grafana UI.
                    type: boolean
                  folderRef:
                    description: FolderRef is a reference to a Folder to populate
                      folderUid.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  folderSelector:
                    description: FolderSelector selects a Folder to populate folderUid.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  folderUid:
                    description: FolderUID is the UID of the folder containing the
                      rule group.
                    type: string
                  intervalSeconds:
                    description: IntervalSeconds is the evaluation interval in seconds.
                    format: int64
                    minimum: 10
                    type: integer
                  name:
                    description: Name is the name of the rule group.
                    type: string
                  orgId:
                    description: |-
                      OrgID is the Organization ID. If not set, the Org ID defined in the
                      provider config will be used.
                    format: int64
                    type: integer
                  orgRef:
                    description: OrgRef is a reference to an Organization to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgSelector:
                    description: OrgSelector selects an Organization to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  rules:
                    description: Rules is the list of alert rules in the group.
                    items:
                      description: Rule represents an individual alert rule within
                        a rule group.
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: Annotations are key-value pairs that provide
                            additional context for the alert.
                          type: object
                        condition:
                          description: Condition is the refId of the query or expression
                            that is the alert condition.
                          type: string
                        data:
                          description: Data contains the queries and expressions that
                            are evaluated to determine the alert state.
                          items:
                            description: RuleQuery represents a query or expression
                              within an alert rule.
                            properties:
                              datasourceUid:
                                description: |-
                                  DatasourceUID is the UID of the datasource to query.
                                  Use "__expr__" for expression queries.
                                type: string
                              model:
                                description: Model contains the query-specific model.
                                  The structure depends on the datasource.
                                type: object
                                x-kubernetes-preserve-unknown-fields: true
                              queryType:
                                description: QueryType specifies the type of query.
                                type: string
                              refId:
                                description: RefID is the unique identifier of the
                                  query within the rule.
                                type: string
                              relativeTimeRange:
                                description: RelativeTimeRange specifies the time
                                  range for the query relative to the evaluation time.
                                properties:
                                  from:
                                    description: From is the start of the time range
                                      in seconds relative to the evaluation time.
                                    format: int64
                                    type: integer
                                  to:
                                    description: To is the end of the time range in
                                      seconds relative to the evaluation time.
                                    format: int64
                                    type: integer
                                required:
                                - from
                                - to
                                type: object
                            required:
                            - datasourceUid
                            - model
                            - refId
                            type: object
                          minItems: 1
                          type: array
                        execErrState:
                          default: Error
                          description: ExecErrState defines the behavior when the
                            alert query fails.
                          enum:
                          - Alerting
                          - Error
                          - OK
                          type: string
                        for:
                          default: 5m
                          description: For is the duration for which the condition
                            must be true before the alert fires.
                          type: string
                        isPaused:
                          description: IsPaused determines whether the alert rule
                            is paused.
                          type: boolean
                        labels:
                          additionalProperties:
                            type: string
                          description: Labels are key-value pairs that are attached
                            to the alert.
                          type: object
                        noDataState:
                          default: NoData
                          description: NoDataState defines the behavior when the alert
                            query returns no data.
                          enum:
                          - Alerting
                          - NoData
                          - OK
                          type: string
                        notificationSettings:
                          description: NotificationSettings configures how notifications
                            are sent for this rule.
                          properties:
                            groupBy:
                              description: GroupBy is a list of labels to group alerts
                                by.
                              items:
                                type: string
                              type: array
                            groupInterval:
                              description: GroupInterval is the minimum time between
                                notifications for a group.
                              type: string
                            groupWait:
                              description: GroupWait is the time to wait before sending
                                the first notification.
                              type: string
                            muteTimeIntervals:
                              description: MuteTimeIntervals is a list of mute time
                                interval names.
                              items:
                                type: string
                              type: array
                            receiver:
                              description: Receiver is the name of the contact point
                                to send notifications to.
                              type: string
                            repeatInterval:
                              description: RepeatInterval is the minimum time between
                                repeated notifications.
                              type: string
                          required:
                          - receiver
                          type: object
                        title:
                          description: Title is the name of the alert rule.
                          type: string
                        uid:
                          description: UID is the unique identifier for the rule.
                            If not specified, Grafana will generate one.
                          type: string
                      required:
                      - condition
                      - data
                      - title
                      type: object
                    minItems: 1
                    type: array
                required:
                - intervalSeconds
                - name
                - rules
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RuleGroupStatus represents the observed state of a RuleGroup.
            properties:
              atProvider:
                description: RuleGroupObservation are the observable fields of a RuleGroup.
                properties:
                  folderUid:
                    description: FolderUID is the UID of the folder containing the
                      rule group.
                    type: string
                  orgId:
                    description: OrgID is the Organization ID.
                    format: int64
                    type: integer
                  rules:
                    description: Rules contains the observed state of the rules.
                    items:
                      description: RuleObservation represents the observed state of
                        a rule.
                      properties:
                        title:
                          description: Title is the name of the alert rule.
                          type: string
                        uid:
                          description: UID is the unique identifier of the rule.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
