---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: notificationpolicies.alerting.gf.m.crossplane.io
spec:
  group: alerting.gf.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gf
    kind: NotificationPolicy
    listKind: NotificationPolicyList
    plural: notificationpolicies
    singular: notificationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NotificationPolicy is the Schema for the NotificationPolicy API.
          It manages the entire notification policy tree for a Grafana organization.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NotificationPolicySpec defines the desired state of a NotificationPolicy.
            properties:
              forProvider:
                description: NotificationPolicyParameters are the configurable fields
                  of a NotificationPolicy.
                properties:
                  contactPoint:
                    description: ContactPoint is the default contact point to use
                      when no routing matches.
                    type: string
                  contactPointRef:
                    description: ContactPointRef is a reference to a ContactPoint
                      to populate contactPoint.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  contactPointSelector:
                    description: ContactPointSelector selects a ContactPoint to populate
                      contactPoint.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  disableProvenance:
                    default: true
                    description: DisableProvenance allows modifying the notification
                      policy from the Grafana UI.
                    type: boolean
                  groupBy:
                    description: |-
                      GroupBy is a list of alert labels to group alerts by.
                      Use "..." to group by all labels.
                    items:
                      type: string
                    type: array
                  groupInterval:
                    description: |-
                      GroupInterval is the minimum time interval between notifications for a group.
                      Default: "5m"
                    type: string
                  groupWait:
                    description: |-
                      GroupWait is the time to wait before sending the first notification for a group.
                      Default: "30s"
                    type: string
                  orgId:
                    description: |-
                      OrgID is the Organization ID. If not set, the Org ID defined in the
                      provider config will be used.
                    format: int64
                    type: integer
                  orgRef:
                    description: OrgRef is a reference to an Organization to populate
                      orgId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  orgSelector:
                    description: OrgSelector selects an Organization to populate orgId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  policy:
                    description: Policy is a list of nested routing policies.
                    items:
                      description: PolicyRoute represents a nested routing policy.
                      properties:
                        activeTimings:
                          description: |-
                            ActiveTimings is a list of active timing names. Alerts are only routed
                            if the current time falls within an active timing.
                          items:
                            type: string
                          type: array
                        contactPoint:
                          description: ContactPoint is the contact point to use for
                            this route.
                          type: string
                        contactPointRef:
                          description: ContactPointRef is a reference to a ContactPoint
                            to populate contactPoint.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        contactPointSelector:
                          description: ContactPointSelector selects a ContactPoint
                            to populate contactPoint.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        continue:
                          description: Continue determines whether to continue matching
                            subsequent routes after this one matches.
                          type: boolean
                        groupBy:
                          description: GroupBy is a list of alert labels to group
                            alerts by for this route.
                          items:
                            type: string
                          type: array
                        groupInterval:
                          description: GroupInterval is the minimum time interval
                            between notifications for this route.
                          type: string
                        groupWait:
                          description: GroupWait is the time to wait before sending
                            the first notification for this route.
                          type: string
                        matchers:
                          description: Matchers is a list of matchers to match alerts
                            against.
                          items:
                            description: PolicyMatcher represents a matcher for routing
                              alerts.
                            properties:
                              label:
                                description: Label is the name of the label to match.
                                type: string
                              match:
                                description: |-
                                  Match is the type of match to perform.
                                  Valid values are: "=", "!=", "=~", "!~"
                                enum:
                                - =
                                - '!='
                                - =~
                                - '!~'
                                type: string
                              value:
                                description: Value is the value to match against.
                                type: string
                            required:
                            - label
                            - match
                            - value
                            type: object
                          type: array
                        muteTimings:
                          description: MuteTimings is a list of mute timing names
                            to apply to this route.
                          items:
                            type: string
                          type: array
                        policy:
                          description: |-
                            Policy is a list of nested routing policies within this route.
                            Note: Maximum nesting depth is 3 levels.
                          items:
                            description: NestedPolicyRoute represents a second-level
                              nested routing policy.
                            properties:
                              activeTimings:
                                description: |-
                                  ActiveTimings is a list of active timing names. Alerts are only routed
                                  if the current time falls within an active timing.
                                items:
                                  type: string
                                type: array
                              contactPoint:
                                description: ContactPoint is the contact point to
                                  use for this route.
                                type: string
                              contactPointRef:
                                description: ContactPointRef is a reference to a ContactPoint
                                  to populate contactPoint.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              contactPointSelector:
                                description: ContactPointSelector selects a ContactPoint
                                  to populate contactPoint.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                              continue:
                                description: Continue determines whether to continue
                                  matching subsequent routes after this one matches.
                                type: boolean
                              groupBy:
                                description: GroupBy is a list of alert labels to
                                  group alerts by for this route.
                                items:
                                  type: string
                                type: array
                              groupInterval:
                                description: GroupInterval is the minimum time interval
                                  between notifications for this route.
                                type: string
                              groupWait:
                                description: GroupWait is the time to wait before
                                  sending the first notification for this route.
                                type: string
                              matchers:
                                description: Matchers is a list of matchers to match
                                  alerts against.
                                items:
                                  description: PolicyMatcher represents a matcher
                                    for routing alerts.
                                  properties:
                                    label:
                                      description: Label is the name of the label
                                        to match.
                                      type: string
                                    match:
                                      description: |-
                                        Match is the type of match to perform.
                                        Valid values are: "=", "!=", "=~", "!~"
                                      enum:
                                      - =
                                      - '!='
                                      - =~
                                      - '!~'
                                      type: string
                                    value:
                                      description: Value is the value to match against.
                                      type: string
                                  required:
                                  - label
                                  - match
                                  - value
                                  type: object
                                type: array
                              muteTimings:
                                description: MuteTimings is a list of mute timing
                                  names to apply to this route.
                                items:
                                  type: string
                                type: array
                              policy:
                                description: |-
                                  Policy is a list of nested routing policies within this route.
                                  Note: This is the final nesting level.
                                items:
                                  description: LeafPolicyRoute represents the deepest
                                    level of routing policy (no further nesting).
                                  properties:
                                    activeTimings:
                                      description: |-
                                        ActiveTimings is a list of active timing names. Alerts are only routed
                                        if the current time falls within an active timing.
                                      items:
                                        type: string
                                      type: array
                                    contactPoint:
                                      description: ContactPoint is the contact point
                                        to use for this route.
                                      type: string
                                    contactPointRef:
                                      description: ContactPointRef is a reference
                                        to a ContactPoint to populate contactPoint.
                                      properties:
                                        name:
                                          description: Name of the referenced object.
                                          type: string
                                        policy:
                                          description: Policies for referencing.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      required:
                                      - name
                                      type: object
                                    contactPointSelector:
                                      description: ContactPointSelector selects a
                                        ContactPoint to populate contactPoint.
                                      properties:
                                        matchControllerRef:
                                          description: |-
                                            MatchControllerRef ensures an object with the same controller reference
                                            as the selecting object is selected.
                                          type: boolean
                                        matchLabels:
                                          additionalProperties:
                                            type: string
                                          description: MatchLabels ensures an object
                                            with matching labels is selected.
                                          type: object
                                        policy:
                                          description: Policies for selection.
                                          properties:
                                            resolution:
                                              default: Required
                                              description: |-
                                                Resolution specifies whether resolution of this reference is required.
                                                The default is 'Required', which means the reconcile will fail if the
                                                reference cannot be resolved. 'Optional' means this reference will be
                                                a no-op if it cannot be resolved.
                                              enum:
                                              - Required
                                              - Optional
                                              type: string
                                            resolve:
                                              description: |-
                                                Resolve specifies when this reference should be resolved. The default
                                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                                the corresponding field is not present. Use 'Always' to resolve the
                                                reference on every reconcile.
                                              enum:
                                              - Always
                                              - IfNotPresent
                                              type: string
                                          type: object
                                      type: object
                                    continue:
                                      description: Continue determines whether to
                                        continue matching subsequent routes after
                                        this one matches.
                                      type: boolean
                                    groupBy:
                                      description: GroupBy is a list of alert labels
                                        to group alerts by for this route.
                                      items:
                                        type: string
                                      type: array
                                    groupInterval:
                                      description: GroupInterval is the minimum time
                                        interval between notifications for this route.
                                      type: string
                                    groupWait:
                                      description: GroupWait is the time to wait before
                                        sending the first notification for this route.
                                      type: string
                                    matchers:
                                      description: Matchers is a list of matchers
                                        to match alerts against.
                                      items:
                                        description: PolicyMatcher represents a matcher
                                          for routing alerts.
                                        properties:
                                          label:
                                            description: Label is the name of the
                                              label to match.
                                            type: string
                                          match:
                                            description: |-
                                              Match is the type of match to perform.
                                              Valid values are: "=", "!=", "=~", "!~"
                                            enum:
                                            - =
                                            - '!='
                                            - =~
                                            - '!~'
                                            type: string
                                          value:
                                            description: Value is the value to match
                                              against.
                                            type: string
                                        required:
                                        - label
                                        - match
                                        - value
                                        type: object
                                      type: array
                                    muteTimings:
                                      description: MuteTimings is a list of mute timing
                                        names to apply to this route.
                                      items:
                                        type: string
                                      type: array
                                    repeatInterval:
                                      description: RepeatInterval is the minimum time
                                        interval between repeated notifications for
                                        this route.
                                      type: string
                                  type: object
                                type: array
                              repeatInterval:
                                description: RepeatInterval is the minimum time interval
                                  between repeated notifications for this route.
                                type: string
                            type: object
                          type: array
                        repeatInterval:
                          description: RepeatInterval is the minimum time interval
                            between repeated notifications for this route.
                          type: string
                      type: object
                    type: array
                  repeatInterval:
                    description: |-
                      RepeatInterval is the minimum time interval between repeated notifications.
                      Default: "4h"
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: NotificationPolicyStatus represents the observed state of
              a NotificationPolicy.
            properties:
              atProvider:
                description: NotificationPolicyObservation are the observable fields
                  of a NotificationPolicy.
                properties:
                  orgId:
                    description: OrgID is the Organization ID.
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
